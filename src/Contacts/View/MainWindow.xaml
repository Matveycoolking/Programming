<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="Contacts" 
        Height="450" 
        Width="800"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            
            <GroupBox Header="Edit Contact" 
                      Grid.Column="0"
                      Padding="10">
                <StackPanel>
                    <TextBlock Text="Name:" 
                               FontSize="12" 
                               FontWeight="Normal"
                               Margin="0,0,0,3"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Phone Number:" 
                               FontSize="12" 
                               FontWeight="Normal"
                               Margin="0,10,0,3"/>
                    <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Email:" 
                               FontSize="12" 
                               FontWeight="Normal"
                               Margin="0,10,0,3"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                </StackPanel>
            </GroupBox>

            <Grid Grid.Column="1"/>
            <GroupBox Header="Read Contact" 
                      Grid.Column="2"
                      Padding="10">
                <StackPanel>
                    <TextBlock Text="Name:" 
                               FontSize="12" 
                               FontWeight="Normal"
                               Margin="0,0,0,3"/>
                    <TextBlock Text="{Binding Name}"
                               Margin="0,1,0,0"
                               FontWeight="Bold"/>

                    <TextBlock Text="Phone Number:" 
                               FontSize="12" 
                               FontWeight="Normal"
                               Margin="0,11,0,3"/>
                    <TextBlock Text="{Binding PhoneNumber}"
                               FontSize="12" 
                               Margin="0, 1, 0, 0"
                               FontWeight="bold"/>

                    <TextBlock Text="Email:" 
                               FontSize="12" 
                               FontWeight="Normal"
                               Margin="0,11,0,3"/>
                    <TextBlock Text="{Binding Email}"
                               FontSize="12" 
                               Margin="0, 1, 0, 0"
                               FontWeight="Bold"/>
                </StackPanel>
            </GroupBox>
        </Grid>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0"/>

            <Grid Grid.Column="1"/>

            <StackPanel Grid.Column="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="Save" 
                        Width="75" 
                        Height="25" 
                        Margin="3,0,3,0"
                        FontSize="12"
                        Command="{Binding SaveCommand}"/>
                <Button Content="Load" 
                        Width="75" 
                        Height="25" 
                        Margin="3,0,0,0"
                        FontSize="12"
                        Command="{Binding LoadCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>